<!DOCTYPE html>
<html>
<head>
  <title>Edit Book</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>

  <div class="form-container">
    <h2>Edit Book page</h2>

    <form action="/books/<%= book._id %>?_method=PUT" method="POST">
      <input type="text" name="title" value="<%= book.title %>" placeholder="Book Title" required>
      <input type="text" name="author" value="<%= book.author %>" placeholder="Author Name" required>
      <input type="number" name="year" value="<%= book.year %>" placeholder="Published Year" required>
      <input type="text" name="img" value="<%= book.img %>" placeholder="Image URL" required>
      <button type="submit">Update Book</button>
    </form>
  </div>

</body>
</html>
